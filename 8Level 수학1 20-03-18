#include <iostream>
#include <string>

using namespace std;

int main()
{
	int a, b, c, cnt = 0;
	cin >> a >> b >> c;

	if (b >= c)
	{
		cout << -1;
		return 0;
	}
	cnt = a / (c - b) + 1;
	cout << cnt;
	return 0;
}
//처음에는 for문으로 작성하였다. 그런데 실패가 떴고 이유가 21억 이하의 수여서 오버플로가 떠서 틀린줄 알았다. 그래서 int형을 더블로 바꿔보았지만
//이번에는 시간초과가 떴다. 그래서 for문을 없애고 다시 작성했다.
---------------------------------------------------------------------------------
